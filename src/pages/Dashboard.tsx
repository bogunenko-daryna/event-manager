import { Paper, Typography } from "@mui/material";

export function DashboardPage() {
  return (
    <Paper sx={{ p: 3 }}>
      <Typography variant="h6">Dashboard</Typography>
      <Typography color="text.secondary">
        Bonus dashboard will be implemented later.
      </Typography>
    </Paper>
  );
}
